import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dailyaddicts.checklist',
  appName: 'Daily Addicts Checklist',
  webDir: 'build'
};

export default config;
